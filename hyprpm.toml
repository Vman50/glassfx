[repository]
name = "glassfx"
authors = ["Vman50"]

[glassfx]
description = "Per-window GLSL shader system for Hyprland"
authors = ["Vman50"]
output = "build/libglassfx.so"
build = ["cmake -B build", "cmake --build build"]
